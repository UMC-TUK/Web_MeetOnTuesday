import styled from 'styled-components';

const WrapPadding = styled.div`
  font-family: Roboto, RobotoDraft, Helvetica, Arial, sans-serif;
  padding-top: 12px;
  display: block;
`;

export default WrapPadding;
